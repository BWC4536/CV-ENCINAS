{
  "installCommand": "npm install",
  "buildCommand": "npm install && ./node_modules/.bin/vite build --root .",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}